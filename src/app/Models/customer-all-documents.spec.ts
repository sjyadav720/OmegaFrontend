import { CustomerAllDocuments } from './customer-all-documents';

describe('CustomerAllDocuments', () => {
  it('should create an instance', () => {
    expect(new CustomerAllDocuments()).toBeTruthy();
  });
});
