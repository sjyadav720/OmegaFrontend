import { CustomerBankDetails } from './customer-bank-details';

describe('CustomerBankDetails', () => {
  it('should create an instance', () => {
    expect(new CustomerBankDetails()).toBeTruthy();
  });
});
