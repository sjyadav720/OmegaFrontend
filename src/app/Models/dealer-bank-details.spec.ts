import { DealerBankDetails } from './dealer-bank-details';

describe('DealerBankDetails', () => {
  it('should create an instance', () => {
    expect(new DealerBankDetails()).toBeTruthy();
  });
});
